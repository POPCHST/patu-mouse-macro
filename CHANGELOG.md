# Changelog

## v1.0.3
- แต่ละจุดในลำดับเลือกได้ว่าจะ **คลิกเมาส์** หรือ **กดคีย์บอร์ด** แทน (เช่น Enter, F5, F1-F12, Space, Esc, Tab)
- เพิ่มช่อง "หรือกดคีย์แทน" + ปุ่ม "เพิ่มการกดคีย์นี้" ในขั้นตอนที่ 2 ผสมจุดคลิกกับจุดกดคีย์ในลำดับเดียวกันได้
- เปลี่ยน "จำนวนคลิก" เป็น "จำนวนการทำงาน" ให้ตรงกับที่นับทั้งคลิกและกดคีย์

## v1.0.2
- เพิ่มระบบ **คลิกหลายจุดตามลำดับ**: กด F8 ซ้ำๆ เพื่อเพิ่มจุดคลิกได้หลายจุด แต่ละจุดตั้งเวลาหน่วง (delay) ของตัวเองได้ โปรแกรมจะคลิกวนตามลำดับที่เพิ่มไว้
- เพิ่มรายการจุดคลิก (list) พร้อมปุ่มลบจุดที่เลือก/ล้างทั้งหมด
- "ทำซ้ำจำนวน" เปลี่ยนความหมายเป็น "จำนวนรอบ" (1 รอบ = คลิกครบทุกจุดในลำดับ 1 รอบ)
- แสดงเลขเวอร์ชันที่ชื่อหน้าต่างโปรแกรม

## v1.0.1
- เพิ่ม **สุ่มเวลาคลิกเล็กน้อย** (±15%) กันรูปแบบเวลาคลิกเป๊ะเกินไป
- เปลี่ยนชื่อโปรแกรมเป็น `patu.v1-mouse-macro` (ทั้งชื่อไฟล์ .exe และหน้าต่าง)
- ใส่ไอคอนโปรแกรม และเปลี่ยนฟอนต์เป็น Tahoma ให้อ่านภาษาไทยง่ายขึ้น
- ออกแบบ UI ใหม่เป็นขั้นตอนที่ 1-4 ชัดเจน พร้อมปุ่ม "❓ วิธีใช้" เปิดหน้าต่างอธิบายวิธีใช้งาน
- เพิ่มแจ้งเตือนเมื่อหยุดทำงานแบบผิดปกติ (หาหน้าต่างเกมไม่เจอ): ข้อความสีแดง + กระพริบ taskbar + เสียงเตือน

## v1.0.0
- เวอร์ชันแรก: auto-clicker พื้นฐาน คลิกได้ทั้งแบบตามตำแหน่งเมาส์ปัจจุบัน และตำแหน่งคงที่ (ยึดกับ % ของหน้าต่างเกม กันเบี้ยวเวลาหน้าต่างย้าย/เครื่องอื่นจอไม่เท่ากัน)
- ใช้ `SendInput` ระดับ OS ให้ทำงานได้กับเมาส์ทุกยี่ห้อ
- Global hotkeys F6=Start, F7=Stop, F8=จับตำแหน่ง (ใช้ได้แม้โฟกัสอยู่ในเกม)
- Publish แบบ self-contained single-file ใช้ได้โดยไม่ต้องลง .NET เพิ่ม
